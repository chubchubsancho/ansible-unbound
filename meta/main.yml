---
galaxy_info:
  author: Grégory SANCHEZ
  description: Ansible role to install and configure unbound
  company: chubchubsancho
  role-name: unbound

  license: MIT

  min_ansible_version: 2.9

  # If this a Container Enabled role,
  # provide the minimum Ansible Container version.
  # min_ansible_container_version:

  # Optionally specify the branch Galaxy will use when accessing the GitHub
  # repo for this role. During role install, if no tags are available,
  # Galaxy will use this branch. During import Galaxy will access files on
  # this branch. If Travis integration is configured,
  # only notifications for this
  # branch will be accepted. Otherwise, in all cases,
  # the repo's default branch
  # (usually master) will be used.
  # github_branch:

  #
  # platforms is a list of platforms,
  # and each platform has a name and a list of versions.
  #
  platforms:
    - name: Debian
      versions:
        - stretch
        - bionic
    - name: Ubuntu
      versions:
        - focal
  galaxy_tags:
    - unbound

dependencies: []
# List your role dependencies here, one per line.
# Be sure to remove the '[]' above,
# if you add dependencies to this list.
